---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Kontakt | NexusDigital">
  <Header />
  <main class="pt-28 pb-20 bg-background-light dark:bg-background-dark min-h-screen">
    <section class="max-w-7xl mx-auto px-6">
      <div class="max-w-3xl mb-12">
        <span class="inline-flex items-center gap-2 bg-primary/10 border border-primary/20 rounded-full px-4 py-1.5 mb-6 text-xs font-bold uppercase tracking-wider text-primary">
          Kontakt
        </span>
        <h1 class="text-4xl md:text-6xl font-black tracking-tight mb-6">Pojďme probrat váš webový projekt</h1>
        <p class="text-slate-600 dark:text-slate-400 text-lg leading-relaxed">
          Napište nám základní informace a do 24 hodin se vám ozveme s návrhem dalšího postupu.
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        <div class="lg:col-span-2 bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-8">
          <h2 class="text-2xl font-black mb-6">Kontaktní formulář</h2>
          <form class="space-y-4">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <input class="w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 px-4 py-3 outline-none focus:border-primary" type="text" placeholder="Jméno" />
              <input class="w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 px-4 py-3 outline-none focus:border-primary" type="email" placeholder="E-mail" />
            </div>
            <input class="w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 px-4 py-3 outline-none focus:border-primary" type="text" placeholder="Firma / projekt" />
            <textarea class="w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 px-4 py-3 outline-none focus:border-primary resize-y" rows="6" placeholder="Popište stručně, co potřebujete"></textarea>
            <button type="submit" class="bg-primary text-white font-bold px-7 py-3 rounded-xl hover:bg-primary/90 transition-all">
              Odeslat poptávku
            </button>
          </form>
        </div>

        <aside class="space-y-4">
          <div class="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-6">
            <h3 class="font-black text-lg mb-2">E-mail</h3>
            <p class="text-slate-600 dark:text-slate-400">hello@nexusdigital.cz</p>
          </div>
          <div class="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-6">
            <h3 class="font-black text-lg mb-2">Telefon</h3>
            <p class="text-slate-600 dark:text-slate-400">+420 777 000 000</p>
          </div>
          <div class="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-6">
            <h3 class="font-black text-lg mb-2">Odezva</h3>
            <p class="text-slate-600 dark:text-slate-400">Obvykle odpovídáme do 24 hodin v pracovní dny.</p>
          </div>
        </aside>
      </div>
    </section>
  </main>
  <Footer />
</Layout>
